# Database Configuration
# Option 1 - Docker (recommended for local dev):
#   Run `docker-compose up -d` then use:
DATABASE_URL=postgresql://medimo:medimo@localhost:5432/medimo

# Option 2 - Neon Serverless:
# DATABASE_URL=postgresql://user:password@ep-xxx.us-east-1.aws.neon.tech/medimo?sslmode=require

# Option 3 - Local PostgreSQL install:
# DATABASE_URL=postgresql://postgres:yourpassword@localhost:5432/medimo

# JWT Secret (generate a strong random string for production)
# In production, use: openssl rand -hex 32
JWT_SECRET=your-super-secret-jwt-key-change-in-production

# Server
PORT=3001
NODE_ENV=development
